{
  "type": "array",
  "items": {
    "type": "object",
    "$schema": "http://json-schema.org/draft-04/schema",
    "required": ["id", "key"],
    "properties": {
      "id": { "type": "number" },
      "key": {
        "type": "string",
        "enum": ["A", "B"]
      }
    }
  }
}
