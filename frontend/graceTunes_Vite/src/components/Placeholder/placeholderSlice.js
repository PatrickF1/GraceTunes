import { createSlice } from "@reduxjs/toolkit"

// Create a slice with an initial state and no reducers since it's a placeholder.
const placeholderSlice = createSlice({
  name: "placeholder",
  initialState: {},
  reducers: {
    // Placeholder action: Actual actions will be defined here later.
  },
})

export default placeholderSlice.reducer
